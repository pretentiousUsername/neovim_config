The `plugins/` directory contains configurations for all of the plugins written
for `lazy.nvim`. That's probably where you want to go.

There are two configurations for autocomplete right now, which will be swapped
out—at the moment, `blink_autocomplete.lua` isn't in use since I need to write
a supercollider source for it, while `nvimcmp_autocomplete.lua` *is* being used.
